# Experiment Fleet — Spec

> **작성일:** 2026-07-16 · **작성자:** Power-xc
> **상태:** Wave 0·1 구현 완료 · Wave 2+ 사용자 검토 대기
> **입력:** [research.md](research.md) · [ADR-0002](../../docs/adrs/0002-experiment-fleet.md)

## 1. 정의

실험의 단위를 "가설 1개 → A/B 실험 1개"에서 "가설 공간 1개 → 사전 등록된 변형 함대"로 확장한다. AI가 변형 후보를 대량 생성하는 시대에 사람의 역할은 변형을 고르는 것에서 **정책(threshold·표본 예산·guardrail·종료 규칙)을 설계하고 최종 결정을 기록하는 것**으로 이동한다. 함대의 컷·승급·수렴은 전부 결정적 TypeScript 코드가 계산한다.

```text
가설 공간 → 함대 사전 등록 (정책 + 변형 N개)
→ 웨이브 측정 (공유 기준선 + 변형별 관찰)
→ 결정적 판정: guardrail 컷 → 표본 부족 재수집 → 원시 delta 순위 → keepShare 컷
→ 다음 웨이브 배분 (생존 변형 + 남은 예산) 또는 중단 (수렴·소진·전멸)
→ 사람의 결정 (승격 후보는 제안일 뿐, Decision이 최종)
```

## 2. 불변 조건

- 기존 8단계 Measure Loop와 단일 실험 흐름을 변경하지 않는다. 함대는 실험 단계의 **선택적 확장**이다.
- 변형별 판정은 기존 `evaluateExperiment`를 그대로 재사용한다. 함대 엔진은 수치를 새로 계산하지 않고 순위·컷·배분만 더한다.
- AI는 변형 후보(변경안 텍스트, 근거 evidence ID 참조)만 제안할 수 있다. 관찰 수치·판정·순위·예산은 AI가 생성·수정할 수 없다.
- 통계적 유의성·p-value·FDR을 계산하거나 흉내 내지 않는다. 판정은 사전 등록된 practical threshold와 표본·기간 규율을 따른다.
- guardrail 위반은 delta·표본과 무관하게 컷한다(안전 우선). 표본 부족은 컷이 아니라 재수집이다(근거 없이 탈락 없음).
- 예산 초과는 숨기지 않고 음수로 노출한다.
- 승격 후보(`promotionCandidateId`)는 제안이며, 최종 결정은 사람이 Decision으로 기록해야 저장된다.

## 3. 범위 (Wave)

| Wave | 내용 | 산출 | 상태 |
|---|---|---|---|
| 0 | 결정적 함대 엔진 — 사전 등록 검증·웨이브 판정·다음 웨이브 배분 | `src/entities/fleet/` + `src/features/experiment-fleet/lib/` + 단위 테스트 | **구현** |
| 1 | 워크스페이스 통합 — 스키마 v3(선택적 `fleet`), 실험 단계 내 함대 섹션 UI, 리포트 확장, 노출 불균형 경고 | v2→v3 무손실 마이그레이션 + 재계산 검증 + widgets + E2E | **구현** |
| 2 | harness 연동 — `fleet` 측정 스킬 활성화: funnel×segments capability로 변형별 관찰 자동 수집. 영구 holdout·승격 확정 웨이브 설계 포함 | 어댑터 쿼리 + 스킬 available 전환 | 검토 대기 |
| 3 | AI 변형 후보 생성 — 기존 AI trust boundary 안에서 evidence 참조 변형 후보 제안 | 후보 생성 route + 검증 | 검토 대기 |

제외(불변): 밴딧 자동 배분, AI 승자 선언, 실시간 트래픽 스플리터, feature flag 엔진, 통계적 유의성 판정.

## 4. 계약 결정

- **공유 기준선** — 한 웨이브는 하나의 기준선(RateCount)을 모든 변형이 공유한다. 다중 팔 실험의 표준 설계이며 예산 산술을 단순하게 유지한다.
- **guardrail 전량 규칙** — 웨이브에 guardrail 기준선이 있으면 모든 변형 관찰에 guardrail 수치가 있어야 한다. 절반만 측정된 guardrail은 순위를 왜곡하므로 검증 단계에서 거부한다.
- **순위 키** — 반올림 전 원시 delta ↓, 변형 표본 ↓, 변형 ID ↑. 0.1pp 반올림은 대량 변형에서 동률을 양산하므로 원시 값으로 순위를 매기고, 동률은 결정적으로 고정한다.
- **컷 우선순위** — guardrail 위반 → 컷, 표본·기간 미달 → 재수집(needs_sample), not_supported → 컷, 나머지가 순위 대상. keepShare로 상위 ceil(keepShare×N)만 승급하되 최소 1개는 남긴다.
- **예산 산술** — `사용 표본 = 기준선 total + Σ 변형 total`. 다음 웨이브 목표 표본 = `floor(남은 예산 / (활성 변형 + 1))`, 변형별 최소 표본 미달이면 소진 선언.
- **수렴 규칙** — 승급 1개 + 재수집 0개 = 수렴(사람 결정 단계로). 재수집만 1개 남으면 수렴이 아니라 계속 수집.
- **노출 불균형 신호** — 변형 표본이 웨이브 중앙값의 절반 미만이거나 2배를 초과하면 `exposureWarnings`에 표시한다. SRM 통계 검정의 결정적 대체이며 판정·순위는 바꾸지 않는다(OSS 조사 §6).
- **저장 무결성** — 함대 상태는 웨이브 입력과 함께 저장되고, 백업 복원 시 판정을 재계산해 저장값과 대조한다. 위조된 판정은 스키마 경계에서 거부된다.

## 5. Acceptance Criteria

| AC | 기준 | 검증 |
|---|---|---|
| FAC-01 | 함대의 변형별 판정 수치는 동일 입력의 `evaluateExperiment` 결과와 정확히 일치한다 | FLEET-WAVE-001 |
| FAC-02 | guardrail 위반 변형은 최고 delta여도, 표본 부족과 겹쳐도 컷된다 | FLEET-WAVE-003 |
| FAC-03 | 표본 부족 변형은 컷되지 않고 다음 웨이브로 재수집된다 | FLEET-WAVE-001 · FLEET-NEXT-005 |
| FAC-04 | 순위는 원시 delta 기준이며 tie-break까지 결정적이다 | FLEET-WAVE-002 |
| FAC-05 | 예산 사용·잔여가 투명하게 계산되고 초과는 음수로 드러난다 | FLEET-WAVE-004 |
| FAC-06 | 수렴·예산 소진·전멸 시 진행을 멈추고 사유를 반환한다 | FLEET-NEXT-002~004 |
| FAC-07 | 사전 등록 검증을 통과하지 못한 함대는 어떤 판정도 실행할 수 없다 | FLEET-PLAN-001~006 · FLEET-WAVE-005 |
| FAC-08 | 기존 단위 테스트·E2E·품질 게이트가 계속 통과한다 | 전체 게이트 |
| FAC-09 | 노출 불균형은 판정을 바꾸지 않고 경고로만 표시된다 | FLEET-WAVE-007 |
| FAC-10 | v1·v2 workspace는 무손실로 v3로 승격되고 승격 전 원본이 백업된다 | HAC-01 · STORAGE-010 |
| FAC-11 | 백업 JSON의 위조된 함대 판정은 재계산 대조로 거부된다 | FLEET-SCHEMA-001~004 |
| FAC-12 | 함대 UI에서 사전 등록 → 웨이브 판정 → 이력 복구가 동작한다 | e2e/fleet.spec.ts |

## 6. 사용자 결정 필요 (Wave 1 전)

| ID | 결정 | 권장 |
|---|---|---|
| D-201 | Project 스키마 v3 (선택적 `fleetPlan` 필드) 확장 시점 | Wave 1 착수 시 |
| D-202 | 함대 UI를 기존 실험 단계 패널에 통합할지 별도 패널로 둘지 | 별도 패널 |
| D-203 | AI 변형 후보 생성의 변형 수 상한 (비용·검토 부담) | 웨이브당 20개 |

## 7. 리스크

- **다중 비교 착시** — N개 중 K개가 threshold를 넘었다는 표시는 보정 없는 다중 비교다. UI·리포트에 "동시 판정 N건" 한계 병기를 Wave 1 요구사항으로 둔다.
- **승자의 저주** — 웨이브 순위와 최종 승격을 같은 표본으로 판단하면 효과가 과대 추정된다. 승격 후보의 최종 확인 웨이브(신규 표본)를 stopRule 권장 패턴으로 문서화한다.
- **novelty effect** — 웨이브 기간(plannedDaysPerWave)이 짧으면 새로움 효과가 순위를 왜곡한다. 기본값 가이드로 완화한다.
- **예산 파편화** — maxActiveVariants와 예산 검증이 방어하지만, 사용자가 과도한 변형 수를 등록하는 것 자체는 막지 않는다(사전 등록 검증이 첫 웨이브 최소 예산을 강제).
